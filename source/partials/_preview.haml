%header
  %h3= link_to article.title, article
  = partial 'partials/blog_stamp', locals: { date: article.date }
.content-article
  - if full ||= false
    = find_and_preserve(article.body)
  - else
    %p= article.data.preview
